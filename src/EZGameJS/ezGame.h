#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include <math.h>

typedef shortcuts(keywords) {
}

typedef makeExe(file, make, compiler) {
}

typedef compile(check) {
}

typedef test(testCompile, exit)  {
}

typedef sprites(model, anim, movement) {
}

typedef ThreeD(render, complete_3d) {
}

typedef TwoD(render_2dimensionals) {
}
