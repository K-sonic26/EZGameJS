#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include <math.h>

typedef render(ren_all) {
}

typedef sprites(spts) {
}

typedef animation(anim) {
}

typedef model(models) {
}

typedef colors(multi, mix, hex) {
}
